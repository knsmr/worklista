= form_for(item, :url => "/users/#{user.id}/items/#{item.id}") do |f|
  .inline-edit
    %p
      Title
      = f.text_field :title, :size => 80
    %p{:style => 'font-size: 0.8em; color: grey'}
      = item.url
    %p
      Tags
      = f.text_field :tag_names
    %p
      Date
      = f.text_field :published_at
    %p= f.submit "Update"
